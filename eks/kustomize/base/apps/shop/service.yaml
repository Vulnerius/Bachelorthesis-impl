---
apiVersion: v1
kind: Service
metadata:
  name: shop-service
  namespace: thesis
  labels:
    app: shop
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: shop