# 

## CICD

Zusätzliche CD-Pipeline für Deployment der fehlerhaften Version, anschließend export von k6-summary und erfassten prometheus daten

###


Szenario-Anpassung:

Ramping Arrival Tests

jvm -> 2 minuten warmup phase
--> 20 minuten last (10-30-60-80-100)
Promote/Fail

analysistemplate:
initialDelay: 2m

verringert möglicherweise die pod-switch latencies (mehr Pods obv auch)

generell verwendung eines horizontal pod autoscalers in verbindung mit managedNodeGroups - damit würde nur hier explizit der vergleich hinken

Query-Vergleich

Latenzen
avg(avg_over_time(k6_http_req_duration_p95{group="::transaction", status="200"}[1m])) 
vs
avg(avg_over_time(ktor_http_server_requests_seconds{quantile="0.95", pod=~".*transactuion-service-rollout.*", status="200"}[1m]))  * 1000
